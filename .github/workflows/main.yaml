on:
  push:
    branches:
      - 'main'
  pull_request:
  merge_group:

name: CI
jobs:

  submit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        design: ['mcu_soc', 'minimal']
    steps:
      - name: Check out source code
        uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Set up PDM
        uses: pdm-project/setup-pdm@v4
        with:
          python-version-file: pyproject.toml
          cache-dependency-path: pyproject.toml
          cache: true

      - name: Generate overrides to use current branch if PR
        if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
        run: |
          pdm run tools/gen_overrides.py > overrides.txt

      - name: Relock PDM
        if: github.event_name != 'pull_request' && github.event_name != 'merge_group'
        run: pdm lock -d

      - name: Relock PDM (PR)
        if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
        run: pdm lock -d --overrides overrides.txt

      - name: Install dependencies
        run: |
          pdm install

      - name: Lock pins
        working-directory: ./${{ matrix.design }}
        run: pdm run chipflow pin lock

      - name: Submit to cloud backend
        working-directory: ./${{ matrix.design }}
        run: pdm run chipflow silicon submit --wait
        env:
          CHIPFLOW_API_KEY: ${{ secrets.CHIPFLOW_API_KEY}}

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        design: ['mcu_soc', 'minimal']
    steps:
      - name: Check out source code
        uses: actions/checkout@v4

      - name: Set up PDM
        uses: pdm-project/setup-pdm@v4
        with:
          python-version-file: pyproject.toml
          cache-dependency-path: pyproject.toml
          cache: true

      - name: Generate overrides to use current branch if PR
        if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
        run: |
          pdm run tools/gen_overrides.py > overrides.txt

      - name: Relock PDM
        if: github.event_name != 'pull_request' && github.event_name != 'merge_group'
        run: pdm lock -d

      - name: Relock PDM (PR)
        if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
        run: pdm lock -d --overrides overrides.txt

      - name: Install dependencies
        run: |
          pdm install

      - name: Cache YoWASP build products
        uses: actions/cache@v4
        with:
          path: ~/.cache/YoWASP
          key: YoWASP-${{ runner.os }}-${{ hashFiles('./.venv/**/*.wasm') }}
          restore-keys: |
            YoWASP-${{ runner.os }}-

      - name: Lock pins
        working-directory: ./${{ matrix.design }}
        run: pdm run chipflow pin lock

      - name: Run unit tests
        working-directory: ./${{ matrix.design }}
        run: |
          pdm test

      - name: Run simulation tests
        working-directory: ./${{ matrix.design }}
        run: |
          make sim-check

      - name: Test submit dry run
        working-directory: ./${{ matrix.design }}
        run: |
          pdm run chipflow silicon submit --dry-run
